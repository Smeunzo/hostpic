extends layout
block title
    | Accueil

block connexionBtn
    a(href="/auth/login" class="btn btn-success") Connexion

block content
    div(class="jumbotron")
        div(class="row justify-content-center")
            h3(class="text-center").
                    Website under construct ! #[br]
                    But the others pages still work. #[br]
                    My Github #[a(href="https://github.com/Smeunzo") here ]